<%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <div class="d-flex justify-content-center">
    <%= f.submit "Create your list!", class: "btn btn-success" %>
  </div>
<% end %>
